# Core pipeline dependencies
# torch and onnxruntime are intentionally unpinned here so both
# Dockerfile (CPU) and Dockerfile.gpu (CUDA) can substitute the right variant.
# See Dockerfile.gpu for the GPU-specific torch+cu121 and onnxruntime-gpu installs.

flask==3.0.3
numpy==1.26.4
opencv-python==4.9.0.80
python-dotenv==1.0.1
psycopg[binary]==3.1.19
ultralytics==8.2.18
deep-sort-realtime==1.3.2
insightface==0.7.3
onnxruntime==1.17.3
torch==2.2.2
